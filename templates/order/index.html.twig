{% extends 'base.html.twig' %}

{% block title %}Valider ma commande! - E-commerce Sadio-shop{% endblock %}

{% block content %}
        <h2> Je passe ma commande</h2>
        <hr>
        <div class="row">
            <div class="col-md-6">
                 {% set formathtml  %}
                    {{ form_start(form, {action:path('order_recap')}) }}
                        {{ form_label(form.addresses, 'Choisissez votre adresse de livraison') }}
                        <a href="{{path('account_address_add') }}">Ajouter une nouvelle adresse de livraison</a>
                    {{ form_end(form) }}
                 {% endset %}

                {{ formathtml|replace({'[br]' : '<br>'})|raw  }}
            </div>

            <div class="col-md-6">
                <diV class="text-center">
                    <b>Récaputilation de ma commande</b><br>
                </diV>        
                <div class="order-summary"> 
                    {% for key,product in cart %}
                        <div class="row {% if key > 0  %}mt-2{% endif %}">
                            <div class="col-2">
                                <img src="/uploads/{{ product.product.illustration }}" alt="{{ product.product.name}}" height="75px"><br>
                            </div>
                            <div class="col-8 my-auto">
                                {{ product.product.name}} <br>
                                <small>{{ product.product.subtitle }}</small>
                            </div>
                            <div class="col-2 my-aut">
                                x {{ product.quantity}}
                            </div>
                        </div>
                    {% endfor %}
                </div>

                <b>Bouton pour payer</b>
            </div>
        </div>
{% endblock %}
